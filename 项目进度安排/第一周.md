# 🗓️ 第一周：环境搭建与项目初始化 - 详细执行方案

## 📋 **第一周任务清单**

| 任务 | 预计用时 | 交付物 | 检查标准 |
|------|----------|--------|----------|
| **Day 1-2**: 环境配置 | 8-10小时 | 开发环境 | 所有软件安装成功，版本符合要求 |
| **Day 3**: 项目初始化 | 4-6小时 | 项目框架 | 前后端项目创建完成，基础结构搭建 |
| **Day 4**: 基础依赖安装 | 3-4小时 | 依赖包文件 | 依赖包安装成功，无错误 |
| **Day 5**: 数据库设计 | 4-6小时 | SQL脚本 | 数据库表设计合理，脚本可执行 |

---

## 🔧 **任务1：环境配置（Day 1-2）**

### **1.1 Python 3.9+ 安装配置**

#### **Windows系统**
```powershell
# 1. 下载安装包
# 访问 https://www.python.org/downloads/
# 下载 Python 3.9+ Windows installer (64-bit)

# 2. 安装注意事项：
# ☑️ Add Python to PATH (重要！)
# ☑️ Install launcher for all users
# ☑️ Customize installation → 全选所有功能

# 3. 验证安装
python --version
# 输出：Python 3.9.13 或更高

# 4. 升级pip
python -m pip install --upgrade pip

# 5. 安装virtualenv（虚拟环境管理）
pip install virtualenv
```

#### **macOS系统**
```bash
# 方法一：使用Homebrew（推荐）
brew install python@3.9

# 方法二：官方安装包
# 访问 https://www.python.org/downloads/macos/

# 验证安装
python3 --version
# 输出：Python 3.9.x

# 设置别名（可选）
echo "alias python=python3" >> ~/.zshrc
source ~/.zshrc
```

#### **Linux系统（Ubuntu/Debian）**
```bash
# 1. 更新包管理器
sudo apt update && sudo apt upgrade -y

# 2. 安装Python 3.9
sudo apt install python3.9 python3.9-venv python3.9-dev -y

# 3. 安装pip
sudo apt install python3-pip -y

# 4. 验证安装
python3.9 --version

# 5. 设置默认Python版本（可选）
sudo update-alternatives --install /usr/bin/python python /usr/bin/python3.9 1
```

### **1.2 Node.js 16+ 安装配置**

#### **Windows/macOS/Linux通用（推荐）**
```bash
# 方法一：使用nvm（Node版本管理器）
# 1. 安装nvm
# Windows: https://github.com/coreybutler/nvm-windows/releases
# macOS/Linux:
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 2. 重新打开终端，验证nvm
nvm --version

# 3. 安装Node.js 16
nvm install 16

# 4. 使用Node.js 16
nvm use 16

# 5. 验证安装
node --version  # 应显示 v16.x.x
npm --version   # 应显示 8.x.x+

# 方法二：直接安装（简单）
# 访问 https://nodejs.org/ 下载LTS版本
```

#### **额外工具安装**
```bash
# 安装yarn（可选）
npm install -g yarn

# 安装pnpm（可选，速度更快）
npm install -g pnpm

# 安装Vue CLI（可选，Vite更推荐）
npm install -g @vue/cli
```

### **1.3 MySQL安装配置**

#### **Windows系统**
```powershell
# 方法一：MySQL Installer（推荐）
# 1. 访问 https://dev.mysql.com/downloads/installer/
# 2. 下载 MySQL Installer for Windows
# 3. 安装时选择：
#    - MySQL Server 8.0
#    - MySQL Workbench (图形化管理工具)
#    - Connector/J, Connector/Python 等
# 4. 设置root密码（记住密码！）
# 5. 配置服务，设为开机启动

# 方法二：XAMPP（集成环境，简单）
# 1. 访问 https://www.apachefriends.org/
# 2. 下载XAMPP并安装
# 3. 启动Apache和MySQL服务

# 验证安装
mysql --version
```

#### **macOS系统**
```bash
# 方法一：Homebrew（推荐）
brew install mysql

# 启动MySQL服务
brew services start mysql

# 安全配置
mysql_secure_installation

# 方法二：MySQL官方DMG
# 访问 https://dev.mysql.com/downloads/mysql/
```

#### **Linux系统（Ubuntu）**
```bash
# 1. 添加MySQL APT仓库
wget https://dev.mysql.com/get/mysql-apt-config_0.8.24-1_all.deb
sudo dpkg -i mysql-apt-config_0.8.24-1_all.deb
sudo apt update

# 2. 安装MySQL
sudo apt install mysql-server mysql-client -y

# 3. 安全配置
sudo mysql_secure_installation

# 4. 启动服务
sudo systemctl start mysql
sudo systemctl enable mysql

# 5. 登录测试
sudo mysql -u root -p
```

#### **配置MySQL（所有系统通用）**
```sql
-- 登录MySQL
mysql -u root -p

-- 创建项目数据库
CREATE DATABASE history_qa CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建项目专用用户
CREATE USER 'history_user'@'localhost' IDENTIFIED BY 'History@2023';

-- 授权
GRANT ALL PRIVILEGES ON history_qa.* TO 'history_user'@'localhost';
GRANT PROCESS ON *.* TO 'history_user'@'localhost';
FLUSH PRIVILEGES;

-- 退出
EXIT;
```

### **1.4 ChromaDB安装**

```bash
# ChromaDB是一个Python包，安装很简单
pip install chromadb

# 可选：安装额外的依赖
pip install sentence-transformers  # 用于本地向量生成
pip install tiktoken  # OpenAI tokenizer

# 验证安装
python -c "import chromadb; print('ChromaDB version:', chromadb.__version__)"
```

### **1.5 开发工具安装**

#### **Visual Studio Code（推荐）**
```bash
# 1. 下载安装：https://code.visualstudio.com/
# 2. 安装必要扩展：
#    - Python (Microsoft)
#    - Vue Language Features (Volar)
#    - Auto Rename Tag
#    - ESLint
#    - Prettier
#    - GitLens
#    - Thunder Client (API测试)
#    - MySQL (数据库管理)
```

#### **Git版本控制**
```bash
# 1. 安装Git
# Windows: https://git-scm.com/download/win
# macOS: brew install git
# Linux: sudo apt install git

# 2. 配置Git
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
git config --global core.autocrlf true  # Windows需要

# 3. 验证安装
git --version
```

---

## 📁 **任务2：项目初始化（Day 3）**

### **2.1 创建项目整体结构**
```bash
# 创建项目根目录
mkdir history-qa-system
cd history-qa-system

# 创建目录结构
mkdir -p backend frontend docs database scripts

# 创建README.md
cat > README.md << 'EOF'
# 中小学历史知识智能问答系统

## 项目简介
基于大模型的RAG智能问答系统，专为中小学历史教育设计。

## 技术栈
- 前端：Vue 3 + Vite + TypeScript + Element Plus
- 后端：Python FastAPI + ChromaDB + MySQL
- AI：百度文心/阿里通义 + RAG架构

## 项目结构
history-qa-system/
├── backend/          # FastAPI后端
├── frontend/         # Vue3前端
├── database/         # 数据库脚本
├── scripts/          # 部署脚本
└── docs/            # 项目文档

## 开发环境
1. Python 3.9+
2. Node.js 16+
3. MySQL 8.0+
4. ChromaDB
EOF
```

### **2.2 初始化后端项目（FastAPI）**
```bash
# 进入backend目录
cd backend

# 创建Python虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 创建后端项目结构
mkdir -p app/{api,core,models,schemas,services,utils,dependencies}
mkdir -p app/api/{endpoints,dependencies}
mkdir -p app/models/{sql,vector}
mkdir -p app/services/{rag,embedding,llm}
mkdir -p tests/{unit,integration}
mkdir -p scripts

# 创建基础文件
touch app/__init__.py
touch app/main.py
touch app/core/__init__.py
touch app/core/config.py
touch app/api/__init__.py
touch app/api/router.py
touch requirements.txt
touch .env.example
touch .gitignore

# 后端.gitignore内容
cat > .gitignore << 'EOF'
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Database
*.db
*.sqlite3

# Environment
.env
*.env.local

# Distribution
dist/
build/
*.egg-info/

# Test
.coverage
htmlcov/
.pytest_cache/
EOF
```

### **2.3 初始化前端项目（Vue 3 + Vite）**
```bash
# 返回项目根目录
cd ../frontend

# 使用Vite创建Vue项目（带TypeScript）
npm create vue@latest . -- --typescript --router --pinia --eslint --prettier

# 安装完成后，安装UI库
npm install element-plus @element-plus/icons-vue
npm install axios

# 创建前端项目结构
mkdir -p src/{api,assets,components,composables,router,stores,utils,views,types}
mkdir -p src/assets/{css,images,fonts}
mkdir -p src/components/{common,layout,business}
mkdir -p src/views/{auth,chat,admin,dashboard}

# 创建.env文件
cat > .env.development << 'EOF'
# 开发环境配置
VITE_API_BASE=http://localhost:8000/api/v1
VITE_APP_TITLE=历史问答系统
VITE_APP_ENV=development
EOF

cat > .env.production << 'EOF'
# 生产环境配置
VITE_API_BASE=/api/v1
VITE_APP_TITLE=历史问答系统
VITE_APP_ENV=production
EOF

# 前端.gitignore内容
cat > .gitignore << 'EOF'
# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/
# Comment in the public line in if your project uses Gatsby and not Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# vuepress build output
.vuepress/dist

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*
EOF
```

### **2.4 初始化Git仓库**
```bash
# 返回项目根目录
cd ..

# 初始化Git仓库
git init

# 创建根目录.gitignore
cat > .gitignore << 'EOF'
# 系统文件
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# 日志文件
*.log
logs/

# 环境变量
.env
*.env.local

# 编译输出
dist/
build/
*.pyc

# 上传的文件
uploads/
temp/
EOF

# 添加所有文件
git add .
git commit -m "初始化项目结构"
```

---

## 📦 **任务3：基础依赖安装（Day 4）**

### **3.1 后端依赖安装**
```bash
# 进入backend目录
cd backend

# 激活虚拟环境（如果还没激活）
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 创建requirements.txt
cat > requirements.txt << 'EOF'
# FastAPI框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# 数据库
sqlalchemy==2.0.23
pymysql==1.1.0
alembic==1.12.1
aiomysql==0.2.0

# 向量数据库
chromadb==0.4.18
sentence-transformers==2.2.2

# AI/ML相关
openai==1.3.0
tiktoken==0.5.1
langchain==0.0.340

# 数据处理
pandas==2.1.3
numpy==1.24.3
pdfplumber==0.10.3
python-docx==0.8.11
jieba==0.42.1

# HTTP客户端
httpx==0.25.1
requests==2.31.0

# 工具类
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0

# 开发工具
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
flake8==6.1.0
mypy==1.7.0
pre-commit==3.5.0
EOF

# 安装依赖
pip install -r requirements.txt

# 可选：导出当前环境
pip freeze > requirements_freeze.txt
```

### **3.2 前端依赖安装**
```bash
# 进入frontend目录
cd ../frontend

# 更新package.json（如果Vite创建时没选某些选项）
cat > package.json << 'EOF'
{
  "name": "history-qa-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts --fix --ignore-path .gitignore",
    "type-check": "vue-tsc --noEmit",
    "format": "prettier --write ."
  },
  "dependencies": {
    "vue": "^3.3.0",
    "vue-router": "^4.2.0",
    "pinia": "^2.1.0",
    "axios": "^1.5.0",
    "element-plus": "^2.3.0",
    "@element-plus/icons-vue": "^2.1.0",
    "nprogress": "^0.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.4.0",
    "@vue/tsconfig": "^0.4.0",
    "typescript": "^5.2.0",
    "vite": "^4.4.0",
    "vue-tsc": "^1.8.0",
    "@types/node": "^20.8.0",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3",
    "@vue/eslint-config-prettier": "^8.0.0",
    "@vue/eslint-config-typescript": "^12.0.0",
    "eslint-plugin-vue": "^9.17.0",
    "unplugin-auto-import": "^0.16.7",
    "unplugin-vue-components": "^0.25.2"
  }
}
EOF

# 安装所有依赖
npm install

# 或者使用yarn
yarn

# 或者使用pnpm（更快）
pnpm install
```

### **3.3 创建基础配置文件**

#### **后端配置**
```bash
cd ../backend

# 创建.env.example
cat > .env.example << 'EOF'
# 应用配置
APP_NAME="历史问答系统"
APP_ENV="development"
DEBUG=true
SECRET_KEY="your-secret-key-here-change-in-production"
ALLOWED_HOSTS=["http://localhost:5173", "http://127.0.0.1:5173"]

# 数据库配置
DATABASE_URL="mysql+pymysql://history_user:History@2023@localhost:3306/history_qa"
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20

# ChromaDB配置
CHROMA_PATH="./data/chroma_db"
CHROMA_HOST="localhost"
CHROMA_PORT=8001

# 大模型API配置
BAIDU_API_KEY=""
BAIDU_SECRET_KEY=""
ALI_API_KEY=""
OPENAI_API_KEY=""

# 嵌入模型配置
EMBEDDING_MODEL="text-embedding-v1"
EMBEDDING_SIZE=384

# 文件上传配置
UPLOAD_DIR="./uploads"
MAX_UPLOAD_SIZE=10485760  # 10MB

# JWT配置
JWT_SECRET_KEY="your-jwt-secret-key"
JWT_ALGORITHM="HS256"
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
EOF

# 复制为开发环境配置
cp .env.example .env
```

#### **前端配置**
```bash
cd ../frontend

# 创建vite.config.ts
cat > vite.config.ts << 'EOF'
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      resolvers: [ElementPlusResolver()],
      imports: ['vue', 'vue-router', 'pinia'],
      dts: 'src/types/auto-imports.d.ts',
    }),
    Components({
      resolvers: [ElementPlusResolver()],
      dts: 'src/types/components.d.ts',
    }),
  ],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src'),
    },
  },
  server: {
    port: 5173,
    host: 'localhost',
    open: true,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, '/api/v1'),
      },
    },
  },
  build: {
    outDir: 'dist',
    sourcemap: false,
    rollupOptions: {
      output: {
        chunkFileNames: 'static/js/[name]-[hash].js',
        entryFileNames: 'static/js/[name]-[hash].js',
        assetFileNames: 'static/[ext]/[name]-[hash].[ext]',
      },
    },
  },
})
EOF
```

---

## 🗃️ **任务4：数据库设计（Day 5）**

### **4.1 MySQL数据库设计**

```bash
# 进入database目录
cd ../database

# 创建SQL脚本目录结构
mkdir -p sql/{tables,views,functions,procedures,data}
mkdir -p migrations/{versions,alembic}
```

#### **设计数据库表（tables.sql）**
```sql
-- database/sql/tables/tables.sql
-- 历史问答系统数据库设计

-- 1. 用户表
CREATE TABLE IF NOT EXISTS users (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '用户ID',
    username VARCHAR(50) UNIQUE NOT NULL COMMENT '用户名',
    email VARCHAR(100) UNIQUE NOT NULL COMMENT '邮箱',
    password_hash VARCHAR(255) NOT NULL COMMENT '密码哈希',
    role ENUM('student', 'teacher', 'admin') DEFAULT 'student' COMMENT '用户角色',
    avatar_url VARCHAR(255) COMMENT '头像URL',
    is_active BOOLEAN DEFAULT TRUE COMMENT '是否激活',
    last_login TIMESTAMP NULL COMMENT '最后登录时间',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_username (username),
    INDEX idx_email (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';

-- 2. 对话会话表
CREATE TABLE IF NOT EXISTS conversations (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '会话ID',
    user_id INT NOT NULL COMMENT '用户ID',
    title VARCHAR(200) DEFAULT '新对话' COMMENT '对话标题',
    session_id VARCHAR(100) UNIQUE NOT NULL COMMENT '会话标识',
    message_count INT DEFAULT 0 COMMENT '消息数量',
    last_message TEXT COMMENT '最后一条消息',
    is_active BOOLEAN DEFAULT TRUE COMMENT '是否活跃',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    INDEX idx_user_id (user_id),
    INDEX idx_session_id (session_id),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='对话会话表';

-- 3. 消息记录表
CREATE TABLE IF NOT EXISTS messages (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '消息ID',
    conversation_id INT NOT NULL COMMENT '会话ID',
    role ENUM('user', 'assistant', 'system') NOT NULL COMMENT '消息角色',
    content TEXT NOT NULL COMMENT '消息内容',
    tokens_used INT DEFAULT 0 COMMENT '使用的token数量',
    processing_time FLOAT DEFAULT 0 COMMENT '处理时间(秒)',
    source_documents JSON COMMENT '来源文档',
    metadata JSON COMMENT '元数据',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    FOREIGN KEY (conversation_id) REFERENCES conversations(id) ON DELETE CASCADE,
    INDEX idx_conversation_id (conversation_id),
    INDEX idx_role (role),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='消息记录表';

-- 4. 知识库文档表
CREATE TABLE IF NOT EXISTS knowledge_documents (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '文档ID',
    title VARCHAR(200) NOT NULL COMMENT '文档标题',
    source_type ENUM('textbook', 'reference_book', 'website', 'paper') DEFAULT 'textbook' COMMENT '来源类型',
    source_name VARCHAR(200) NOT NULL COMMENT '来源名称',
    grade_level VARCHAR(50) COMMENT '年级层次',
    subject_area VARCHAR(100) COMMENT '学科领域',
    file_path VARCHAR(500) COMMENT '文件路径',
    file_size BIGINT DEFAULT 0 COMMENT '文件大小(字节)',
    content_hash VARCHAR(64) UNIQUE COMMENT '内容哈希',
    chunk_count INT DEFAULT 0 COMMENT '分块数量',
    status ENUM('pending', 'processing', 'completed', 'failed') DEFAULT 'pending' COMMENT '处理状态',
    vectorized_at TIMESTAMP NULL COMMENT '向量化时间',
    created_by INT COMMENT '创建者ID',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE SET NULL,
    INDEX idx_source_type (source_type),
    INDEX idx_grade_level (grade_level),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='知识库文档表';

-- 5. 知识库分块表
CREATE TABLE IF NOT EXISTS knowledge_chunks (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '分块ID',
    document_id INT NOT NULL COMMENT '文档ID',
    chunk_index INT NOT NULL COMMENT '分块序号',
    content TEXT NOT NULL COMMENT '分块内容',
    content_length INT NOT NULL COMMENT '内容长度',
    chunk_hash VARCHAR(64) COMMENT '分块哈希',
    vector_id VARCHAR(100) COMMENT '向量ID',
    metadata JSON COMMENT '元数据(包含位置、关键词等)',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    FOREIGN KEY (document_id) REFERENCES knowledge_documents(id) ON DELETE CASCADE,
    UNIQUE KEY uk_document_chunk (document_id, chunk_index),
    INDEX idx_document_id (document_id),
    INDEX idx_vector_id (vector_id),
    INDEX idx_chunk_hash (chunk_hash)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='知识库分块表';

-- 6. 问答反馈表
CREATE TABLE IF NOT EXISTS feedback (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '反馈ID',
    message_id INT NOT NULL COMMENT '消息ID',
    user_id INT NOT NULL COMMENT '用户ID',
    rating TINYINT CHECK (rating >= 1 AND rating <= 5) COMMENT '评分(1-5)',
    feedback_type ENUM('accuracy', 'helpfulness', 'clarity', 'speed', 'other') COMMENT '反馈类型',
    comment TEXT COMMENT '评论',
    is_resolved BOOLEAN DEFAULT FALSE COMMENT '是否已处理',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    INDEX idx_message_id (message_id),
    INDEX idx_user_id (user_id),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='问答反馈表';

-- 7. 系统日志表
CREATE TABLE IF NOT EXISTS system_logs (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '日志ID',
    level VARCHAR(20) NOT NULL COMMENT '日志级别',
    module VARCHAR(100) COMMENT '模块名称',
    action VARCHAR(100) NOT NULL COMMENT '操作',
    user_id INT COMMENT '用户ID',
    ip_address VARCHAR(45) COMMENT 'IP地址',
    user_agent TEXT COMMENT '用户代理',
    details JSON COMMENT '详细信息',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX idx_level (level),
    INDEX idx_module (module),
    INDEX idx_action (action),
    INDEX idx_user_id (user_id),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统日志表';
```

#### **创建初始化脚本（init.sql）**
```bash
cat > sql/init.sql << 'EOF'
-- 数据库初始化脚本
-- 执行顺序：创建数据库 -> 创建用户 -> 导入表结构

-- 1. 创建数据库
DROP DATABASE IF EXISTS history_qa;
CREATE DATABASE history_qa CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 2. 使用数据库
USE history_qa;

-- 3. 创建表（导入上面的tables.sql内容）
-- 这里应该包含上面所有的CREATE TABLE语句

-- 4. 插入初始数据
INSERT INTO users (username, email, password_hash, role) VALUES
('admin', 'admin@history-qa.com', '$2b$12$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx', 'admin'),
('teacher1', 'teacher1@history-qa.com', '$2b$12$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx', 'teacher'),
('student1', 'student1@history-qa.com', '$2b$12$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx', 'student');

-- 5. 创建测试数据
INSERT INTO knowledge_documents (title, source_type, source_name, grade_level, subject_area, status) VALUES
('七年级历史上册', 'textbook', '人教版历史教材', '七年级', '中国历史', 'completed'),
('八年级历史下册', 'textbook', '北师大版历史教材', '八年级', '世界历史', 'completed'),
('中国通史（青少年版）', 'reference_book', '中国通史出版社', '全年级', '综合历史', 'completed');

-- 6. 创建视图
CREATE VIEW v_user_conversations AS
SELECT 
    u.username,
    u.email,
    c.id as conversation_id,
    c.title,
    c.message_count,
    c.created_at as conversation_start,
    c.updated_at as last_activity
FROM users u
JOIN conversations c ON u.id = c.user_id
WHERE c.is_active = TRUE;

CREATE VIEW v_document_statistics AS
SELECT 
    kd.title,
    kd.source_type,
    kd.grade_level,
    kd.status,
    COUNT(kc.id) as chunk_count,
    SUM(kc.content_length) as total_length
FROM knowledge_documents kd
LEFT JOIN knowledge_chunks kc ON kd.id = kc.document_id
GROUP BY kd.id;

-- 7. 创建存储过程
DELIMITER $$

CREATE PROCEDURE sp_get_conversation_history(
    IN p_user_id INT,
    IN p_limit INT
)
BEGIN
    SELECT 
        c.id,
        c.title,
        c.message_count,
        c.last_message,
        c.created_at,
        COUNT(m.id) as total_messages
    FROM conversations c
    LEFT JOIN messages m ON c.id = m.conversation_id
    WHERE c.user_id = p_user_id
    GROUP BY c.id
    ORDER BY c.updated_at DESC
    LIMIT p_limit;
END$$

CREATE PROCEDURE sp_get_daily_stats(
    IN p_date DATE
)
BEGIN
    SELECT 
        COUNT(DISTINCT c.user_id) as active_users,
        COUNT(c.id) as new_conversations,
        COUNT(m.id) as total_messages,
        AVG(m.processing_time) as avg_response_time
    FROM conversations c
    LEFT JOIN messages m ON c.id = m.conversation_id
    WHERE DATE(c.created_at) = p_date;
END$$

DELIMITER ;

-- 8. 创建触发器
DELIMITER $$

CREATE TRIGGER tr_update_conversation_message_count
AFTER INSERT ON messages
FOR EACH ROW
BEGIN
    UPDATE conversations 
    SET 
        message_count = message_count + 1,
        last_message = LEFT(NEW.content, 200),
        updated_at = CURRENT_TIMESTAMP
    WHERE id = NEW.conversation_id;
END$$

DELIMITER ;
EOF
```

#### **创建执行脚本**
```bash
cat > init_database.sh << 'EOF'
#!/bin/bash
# 数据库初始化脚本

echo "开始初始化历史问答系统数据库..."

# 设置变量
DB_HOST="localhost"
DB_PORT="3306"
DB_ROOT_USER="root"
DB_ROOT_PASS="你的root密码"
DB_NAME="history_qa"
DB_USER="history_user"
DB_PASS="History@2023"

# 1. 使用root用户执行初始化脚本
echo "执行SQL初始化脚本..."
mysql -h $DB_HOST -P $DB_PORT -u $DB_ROOT_USER -p"$DB_ROOT_PASS" < sql/init.sql

# 2. 验证数据库创建
echo "验证数据库创建..."
mysql -h $DB_HOST -P $DB_PORT -u $DB_ROOT_USER -p"$DB_ROOT_PASS" -e "SHOW DATABASES LIKE '$DB_NAME';"

# 3. 验证用户权限
echo "验证用户权限..."
mysql -h $DB_HOST -P $DB_PORT -u $DB_USER -p"$DB_PASS" -e "USE $DB_NAME; SHOW TABLES;"

echo "数据库初始化完成！"
EOF

# 给脚本执行权限
chmod +x init_database.sh
```

### **4.2 ChromaDB向量库设计**

```bash
# 创建向量库配置
cd ../backend/app/models/vector

cat > chroma_config.py << 'EOF'
"""
ChromaDB向量库配置
"""
from typing import List, Dict, Any, Optional
import chromadb
from chromadb.config import Settings

class ChromaConfig:
    """ChromaDB配置类"""
    
    # 向量库名称
    COLLECTION_NAME = "history_knowledge"
    
    # 向量维度（根据嵌入模型调整）
    EMBEDDING_DIMENSION = 384  # 百度文心embedding-v1的维度
    
    # 距离度量方法
    DISTANCE_METRIC = "cosine"  # 可选: "l2", "ip", "cosine"
    
    # 元数据字段定义
    METADATA_FIELDS = {
        "document_id": str,      # 文档ID
        "chunk_index": int,      # 分块索引
        "source_type": str,      # 来源类型: textbook, reference_book等
        "source_name": str,      # 来源名称
        "grade_level": str,      # 年级层次
        "subject_area": str,     # 学科领域
        "title": str,            # 文档标题
        "page_number": int,      # 页码（如果适用）
        "keywords": List[str],   # 关键词
        "timestamp": str,        # 时间戳
    }
    
    @classmethod
    def get_chroma_settings(cls, persist_directory: str = "./chroma_db") -> Settings:
        """获取ChromaDB设置"""
        return Settings(
            chroma_db_impl="duckdb+parquet",
            persist_directory=persist_directory,
            anonymized_telemetry=False  # 禁用遥测
        )
    
    @classmethod
    def get_collection_config(cls) -> Dict[str, Any]:
        """获取集合配置"""
        return {
            "name": cls.COLLECTION_NAME,
            "metadata": {
                "hnsw:space": cls.DISTANCE_METRIC,
                "description": "中小学历史知识向量库"
            }
        }

# 嵌入模型配置
class EmbeddingConfig:
    """嵌入模型配置"""
    
    # 本地嵌入模型（备选）
    LOCAL_MODELS = {
        "paraphrase-multilingual-MiniLM-L12-v2": {
            "dimension": 384,
            "language": "multilingual",
            "description": "多语言句子嵌入模型"
        },
        "text2vec-base-chinese": {
            "dimension": 768,
            "language": "chinese",
            "description": "中文文本嵌入模型"
        }
    }
    
    # 云端嵌入模型
    CLOUD_MODELS = {
        "baidu": {
            "model_name": "embedding-v1",
            "dimension": 384,
            "max_tokens": 384,
            "endpoint": "https://aip.baidubce.com/rpc/2.0/ai_custom/v1/wenxinworkshop/embeddings/embedding-v1"
        },
        "ali": {
            "model_name": "text-embedding-v1",
            "dimension": 1536,
            "max_tokens": 2048,
            "endpoint": "https://dashscope.aliyuncs.com/api/v1/services/embeddings/text-embedding/text-embedding"
        }
    }
EOF
```

### **4.3 创建数据库连接配置**

```bash
cd ../../../../backend/app/core

cat > database.py << 'EOF'
"""
数据库连接配置
"""
from typing import AsyncGenerator
from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker, create_async_engine
from sqlalchemy.orm import DeclarativeBase
from .config import settings

class Base(DeclarativeBase):
    """SQLAlchemy基类"""
    pass

class Database:
    """数据库连接管理器"""
    
    def __init__(self):
        self.engine = None
        self.async_session_maker = None
    
    async def init(self):
        """初始化数据库连接"""
        # 创建异步引擎
        self.engine = create_async_engine(
            settings.DATABASE_URL,
            echo=settings.DEBUG,
            pool_size=settings.DATABASE_POOL_SIZE,
            max_overflow=settings.DATABASE_MAX_OVERFLOW,
            pool_pre_ping=True
        )
        
        # 创建会话工厂
        self.async_session_maker = async_sessionmaker(
            self.engine,
            class_=AsyncSession,
            expire_on_commit=False
        )
        
        # 创建所有表
        async with self.engine.begin() as conn:
            await conn.run_sync(Base.metadata.create_all)
    
    async def get_session(self) -> AsyncGenerator[AsyncSession, None]:
        """获取数据库会话"""
        if not self.async_session_maker:
            await self.init()
        
        async with self.async_session_maker() as session:
            try:
                yield session
            finally:
                await session.close()

# 创建全局数据库实例
database = Database()

async def get_db() -> AsyncGenerator[AsyncSession, None]:
    """依赖注入用的数据库会话获取器"""
    async for session in database.get_session():
        yield session
EOF
```

### **4.4 数据库验证脚本**

```bash
cd ../../scripts

cat > verify_database.py << 'EOF'
#!/usr/bin/env python3
"""
数据库验证脚本
"""
import asyncio
import mysql.connector
from mysql.connector import Error
import chromadb
from app.core.config import settings

async def verify_mysql():
    """验证MySQL连接"""
    print("验证MySQL数据库连接...")
    try:
        connection = mysql.connector.connect(
            host='localhost',
            user='history_user',
            password='History@2023',
            database='history_qa'
        )
        
        if connection.is_connected():
            cursor = connection.cursor()
            cursor.execute("SELECT DATABASE();")
            db_name = cursor.fetchone()[0]
            print(f"✓ MySQL连接成功！当前数据库: {db_name}")
            
            # 检查表是否存在
            cursor.execute("SHOW TABLES;")
            tables = cursor.fetchall()
            print(f"✓ 数据库表数量: {len(tables)}")
            
            for table in tables:
                print(f"  - {table[0]}")
            
            cursor.close()
            connection.close()
            return True
            
    except Error as e:
        print(f"✗ MySQL连接失败: {e}")
        return False

def verify_chromadb():
    """验证ChromaDB连接"""
    print("\n验证ChromaDB连接...")
    try:
        client = chromadb.PersistentClient(path=settings.CHROMA_PATH)
        collections = client.list_collections()
        print(f"✓ ChromaDB连接成功！")
        print(f"  数据库路径: {settings.CHROMA_PATH}")
        print(f"  集合数量: {len(collections)}")
        
        if len(collections) > 0:
            for collection in collections:
                count = collection.count()
                print(f"  - {collection.name}: {count} 条记录")
        
        return True
    except Exception as e:
        print(f"✗ ChromaDB连接失败: {e}")
        return False

async def main():
    """主验证函数"""
    print("=" * 50)
    print("数据库环境验证")
    print("=" * 50)
    
    mysql_ok = await verify_mysql()
    chroma_ok = verify_chromadb()
    
    print("\n" + "=" * 50)
    if mysql_ok and chroma_ok:
        print("✓ 所有数据库连接验证通过！")
        return True
    else:
        print("✗ 数据库连接存在问题，请检查配置。")
        return False

if __name__ == "__main__":
    asyncio.run(main())
EOF
```

---

## ✅ **第一周验收检查清单**

### **环境配置验证**
```bash
# 1. 检查Python版本
python --version
# 输出应为: Python 3.9.x 或更高

# 2. 检查Node.js版本
node --version
# 输出应为: v16.x.x 或更高

# 3. 检查MySQL是否运行
mysql --version
# 输出应显示MySQL版本

# 4. 检查ChromaDB安装
python -c "import chromadb; print(f'ChromaDB {chromadb.__version__}')"
```

### **项目结构验证**
```bash
# 检查项目目录结构
tree -L 3 history-qa-system/

# 应看到类似结构：
# history-qa-system/
# ├── backend/
# │   ├── app/
# │   ├── venv/
# │   ├── requirements.txt
# │   └── .env
# ├── frontend/
# │   ├── src/
# │   ├── node_modules/
# │   ├── package.json
# │   └── vite.config.ts
# ├── database/
# │   └── sql/
# └── scripts/
```

### **依赖安装验证**
```bash
# 后端依赖检查
cd backend
source venv/bin/activate  # 或 venv\Scripts\activate
pip list | grep -E "(fastapi|uvicorn|sqlalchemy|chromadb)"

# 前端依赖检查
cd ../frontend
npm list --depth=0 | grep -E "(vue|vite|element-plus|axios)"
```

### **数据库验证**
```bash
# 1. 运行数据库验证脚本
cd backend
python scripts/verify_database.py

# 2. 或手动验证
mysql -u history_user -pHistory@2023 -e "USE history_qa; SHOW TABLES;"
```

### **Git仓库验证**
```bash
# 检查Git提交
git log --oneline
# 应该能看到初始提交

# 检查.gitignore
git status
# 应该看不到venv、node_modules等目录
```

## 📝 **第一周完成标志**

当你完成以下所有检查，就标志着第一周任务圆满完成：

1. ✅ **环境配置**：Python、Node.js、MySQL、ChromaDB全部安装成功
2. ✅ **项目结构**：前后端项目框架创建完成
3. ✅ **依赖安装**：所有依赖包安装无误
4. ✅ **数据库设计**：SQL脚本可执行，数据库表设计合理
5. ✅ **版本控制**：项目已提交到Git仓库

## 🚨 **常见问题解决**

### **问题1：Python虚拟环境激活失败**
```bash
# Windows PowerShell可能阻止脚本执行
# 解决方法：以管理员身份运行PowerShell，执行：
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### **问题2：MySQL连接被拒绝**
```bash
# 检查MySQL服务是否运行
sudo systemctl status mysql  # Linux
# 或
netstat -an | grep 3306      # Windows/Linux

# 检查用户权限
mysql -u root -p -e "SHOW GRANTS FOR 'history_user'@'localhost';"
```

### **问题3：ChromaDB导入错误**
```bash
# 尝试重新安装特定版本
pip uninstall chromadb
pip install chromadb==0.4.18

# 或使用conda安装
conda install -c conda-forge chromadb
```

### **问题4：Vite创建项目失败**
```bash
# 清理缓存重试
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

现在你已经完成了第一周的所有任务！可以开始进入第二周的知识库构建与向量化工作。如果在执行过程中遇到任何问题，随时可以查阅这些详细步骤或寻求帮助。